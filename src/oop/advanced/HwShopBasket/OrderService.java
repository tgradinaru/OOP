package oop.advanced.HwShopBasket;

import java.util.HashMap;
import java.util.Map;
import java.util.concurrent.atomic.AtomicInteger;

public class OrderService {
/*    public static Double totalPrice(Basket basket) {
        Double suma = 0.0;
        for (Map.Entry<GenericProduct, AtomicInteger> element : basket.getBasketList().entrySet()) {
            Double aux = element.getValue().doubleValue();
            suma += element.getValue().doubleValue() * element.getKey().getPrice();
        }
        return suma;
    }

    public static Double totalPrice2(Basket basket) {
        double total =
                basket.getBasketList().entrySet().stream()
                        .map(product -> product.getKey().getPrice() * product.getValue().doubleValue())
                        .reduce(0.0, (currValue, element) -> (currValue + element));
        return total;
    }*/
}





